<!---
 File generated by help_docs.sh. DO NOT EDIT.
 Edit scripts/help_docs.sh to change generated content.
 --->

# arma

The `arma` command launches Arma ordering service nodes: router, batcher, consensus, and assembler.

## arma
```
usage: Arma [<flags>] <command> [<args> ...]

Launches an Arma node (Router | Assembler | Batcher | Consensus)

Flags:
  --help           Show context-sensitive help (also try --help-long and
                   --help-man).
  --config=CONFIG  Specifies the config file to load the configuration from

Commands:
  help [<command>...]
    Show help.

  router
    run a router node

  assembler
    run an assembler node

  batcher
    run a batcher node

  consensus
    run a consensus node
```


## arma router
```
usage: Arma router

run a router node

Flags:
  --help           Show context-sensitive help (also try --help-long and
                   --help-man).
  --config=CONFIG  Specifies the config file to load the configuration from
```


## arma batcher
```
usage: Arma batcher

run a batcher node

Flags:
  --help           Show context-sensitive help (also try --help-long and
                   --help-man).
  --config=CONFIG  Specifies the config file to load the configuration from
```


## arma consensus
```
usage: Arma consensus

run a consensus node

Flags:
  --help           Show context-sensitive help (also try --help-long and
                   --help-man).
  --config=CONFIG  Specifies the config file to load the configuration from
```


## arma assembler
```
usage: Arma assembler

run an assembler node

Flags:
  --help           Show context-sensitive help (also try --help-long and
                   --help-man).
  --config=CONFIG  Specifies the config file to load the configuration from
```

